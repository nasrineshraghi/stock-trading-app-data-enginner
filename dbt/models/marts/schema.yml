version: 2

models:
  - name: fct_stock_daily_returns
    description: Daily OHLCV with day-over-day return percentage per ticker
    columns:
      - name: ticker
        tests:
          - not_null
      - name: date
        tests:
          - not_null
      - name: close
        tests:
          - not_null
