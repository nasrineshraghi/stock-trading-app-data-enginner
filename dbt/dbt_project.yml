name: stock_analytics
version: 1.0.0
config-version: 2

profile: stock_pipeline

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - target
  - dbt_packages

models:
  stock_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
